# ============================================================
# AI STEM Karaoke Studio — PythonAnywhere Optimized Dependencies
# PyTorch comes pre-installed on PythonAnywhere system-wide
# Install: pip install -r requirements-pythonanywhere.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
Flask==3.1.2
Werkzeug==3.1.5
Jinja2==3.1.6
click==8.3.1
blinker==1.9.0
itsdangerous==2.2.0
MarkupSafe==3.0.3

# ── Production Server ────────────────────────────────────────
gunicorn==21.2.0

# ── AI / Audio Separation (Demucs) ───────────────────────────
# NOTE: torch, torchaudio, torchvision are pre-installed on PythonAnywhere
demucs==4.0.1
# torch==2.10.0            # SKIP - use system torch
# torchaudio==2.10.0       # SKIP - use system torchaudio
# torchvision==0.25.0      # SKIP - use system torchvision
torchcodec==0.10.0

# ── Demucs Runtime Dependencies ──────────────────────────────
einops==0.8.2
julius==0.2.7
openunmix==1.3.0
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
dora_search==0.1.12
lameenc==1.8.1

# ── Audio Processing ─────────────────────────────────────────
numpy==2.4.2
tqdm==4.67.3
librosa==0.11.0
mir_eval>=0.7
scipy>=1.13.0

# ── YouTube / URL Download ────────────────────────────────────
yt-dlp==2026.2.4
requests==2.32.5
