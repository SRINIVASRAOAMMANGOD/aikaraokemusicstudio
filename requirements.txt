# ============================================================
# AI STEM Karaoke Studio — Python Dependencies
# Last updated: 2026-03-01
# Install: pip install -r requirements.txt
# ============================================================

# ── Web Framework ────────────────────────────────────────────
Flask==3.1.2
Werkzeug==3.1.5
Jinja2==3.1.6
click==8.3.1
blinker==1.9.0
itsdangerous==2.2.0
MarkupSafe==3.0.3

# ── Production Server ────────────────────────────────────────
gunicorn==21.2.0          # Linux/macOS production WSGI server
# waitress==3.0.0         # Windows-compatible alternative (uncomment if needed)

# ── AI / Audio Separation (Demucs) ───────────────────────────
demucs==4.0.1
torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0
torchcodec==0.10.0

# ── Demucs Runtime Dependencies ──────────────────────────────
einops==0.8.2
julius==0.2.7
openunmix==1.3.0
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
dora_search==0.1.12
lameenc==1.8.1

# ── Audio Processing ─────────────────────────────────────────
numpy==2.4.2
tqdm==4.67.3
librosa==0.11.0           # vocal scoring (pitch / timing / tone analysis)
mir_eval>=0.7             # MIR evaluation metrics (raw pitch accuracy, onset F-measure)
scipy>=1.13.0             # librosa dependency

# ── YouTube / URL Download ────────────────────────────────────
yt-dlp==2026.2.4
requests==2.32.5

# ── Notes ────────────────────────────────────────────────────
# GPU acceleration: install the CUDA-enabled torch wheel for your
# CUDA version from https://pytorch.org/get-started/locally/
# e.g. pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# FFmpeg is required by yt-dlp for YouTube audio extraction.
# Download from https://ffmpeg.org and add to PATH.
